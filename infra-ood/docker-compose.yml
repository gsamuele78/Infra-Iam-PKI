services:
  ood-init:
    build:
      context: .
      dockerfile: Dockerfile.init
    container_name: ood-init
    user: root
    networks: [ ood-net ]
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.2'
    command: >
      -c "mkdir -p /certs /data/ood_config /public && chown -R 1000:1000 /certs /data/ood_config /public && /scripts/infra-iam/fetch_pki_root.sh /certs/root_ca.crt"
    volumes:
      - ./certs:/certs
      - ./data:/data
      - ./scripts:/scripts
      - .env:/app/.env

  ood-portal:
    build:
      context: .
      dockerfile: Dockerfile.ood
    image: infra-ood:local
    container_name: ood-portal
    # Open OnDemand requires root to execute 'sudo' for user-specific app spawning
    user: root
    # privileged: true  # REMOVED: contradicts cap_drop, use specific caps
    restart: unless-stopped
    depends_on:
      ood-init:
        condition: service_completed_successfully
    networks:
      - ood-net
    # RStudio relies on host network in R-studioConf, allowing OOD to hit it directly if bridging allows
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 2048M
          cpus: '2.0'
        reservations:
          memory: 512M
    environment:
      # OIDC Integration Variables mapped from infra-iam
      - OIDC_URI=${OIDC_ENDPOINT}
      - OIDC_CLIENT_ID=${OIDC_CLIENT_ID}
      - OIDC_CLIENT_SECRET=${OIDC_CLIENT_SECRET}
    volumes:
      - ./config/ood_portal.yml:/etc/ood/config/ood_portal.yml:ro
      - ./public/bigea-theme.css:/var/www/ood/public/bigea-theme.css:ro
      - ./certs:/certs:ro
      # Import Internal PKI Root (dynamically fetched by ood-init)
      - ./certs/root_ca.crt:/usr/local/share/ca-certificates/step-ca.crt:ro
      - ./public/img:/var/www/ood/public/img:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    cap_drop: [ ALL ]
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE

networks:
  ood-net:
    name: ood-net
