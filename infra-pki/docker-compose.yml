services:
  # 1. Pre-Start Init: Creates directories and secrets
  # Must run BEFORE step-ca starts.
  init-files:
    image: smallstep/step-ca:0.29.0
    container_name: step-ca-init-files
    user: root
    entrypoint: /bin/sh
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    environment:
      - CA_PASSWORD=${CA_PASSWORD}
      - PUID=${PUID}
      - PGID=${PGID}
    volumes:
      - ./step_data:/home/step
      - ./logs:/opt/logs
      - ./db_data:/db_data
    # Create log directories and ensure permissions for PUID:PGID on all data
    command: -c "mkdir -p /home/step/secrets /opt/logs/step-ca /opt/logs/watchtower /opt/logs/caddy /opt/logs/postgres && printf \"%s\" \"${CA_PASSWORD}\" > /home/step/secrets/password && chown -R ${PUID}:${PGID} /home/step /opt/logs /db_data"

  postgres:
    image: postgres:15-alpine
    container_name: step-ca-db
    restart: unless-stopped
    user: "${PUID}:${PGID}"
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./db_data:/var/lib/postgresql/data
      - ./logs/postgres:/var/log/postgresql
    # Enable file logging in addition to Docker logs
    command: postgres -c logging_collector=on -c log_destination=stderr,csvlog -c log_directory=/var/log/postgresql -c log_filename=postgresql.log
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    logging: &default-logging
      driver: "json-file"
      options:
        max-file: "5"
        max-size: "10m"

  # 2. Main Service
  step-ca:
    image: smallstep/step-ca:0.29.0
    container_name: step-ca
    hostname: step-ca
    user: "${PUID}:${PGID}"
    restart: unless-stopped
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    depends_on:
      init-files:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    # Ports removed - accessed via Caddy usage
    environment:
      DOCKER_STEPCA_INIT_NAME: "Internal Root CA"
      DOCKER_STEPCA_INIT_DNS_NAMES: "localhost,${DOMAIN_CA},step-ca"
      DOCKER_STEPCA_INIT_ACME: "true"
      DOCKER_STEPCA_INIT_REDIS: "false"
      DOCKER_STEPCA_INIT_REMOTE_MANAGEMENT: "true"
      DOCKER_STEPCA_INIT_SSH: "true"
      # FORCE configured password during initialization
      # Use FILE to ensure consistency with configurator (avoids newline mismatches)
      DOCKER_STEPCA_INIT_PASSWORD_FILE: /home/step/secrets/password
      STEP_CA_PASSWORD_FILE: /home/step/secrets/password
      # PostgreSQL Connection
      PGHOST: postgres
      PGPORT: 5432
      PGUSER: ${POSTGRES_USER}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGDATABASE: ${POSTGRES_DB}
    volumes:
      - ./step_data:/home/step
      - ./logs/step-ca:/var/log/step-ca
    entrypoint: /bin/bash -c "ls -la /home/step/secrets; /bin/bash /entrypoint.sh step-ca --password-file=/home/step/secrets/password 2>&1 | tee -a /var/log/step-ca/system.log"
    logging: *default-logging
    healthcheck:
      test: [ "CMD", "step", "ca", "health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # 3. Post-Start Config: Configures provisioners dynamically
  # Must run AFTER step-ca is healthy.
  configurator:
    image: smallstep/step-cli:0.29.0
    container_name: step-ca-configurator
    user: "${PUID}:${PGID}"
    entrypoint: /bin/sh
    restart: on-failure:5
    depends_on:
      step-ca:
        condition: service_healthy
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    environment:
      - STEP_CA_PASSWORD_FILE=/home/step/secrets/password
      # Provisioner Configs
      - OIDC_CLIENT_ID=${OIDC_CLIENT_ID}
      - OIDC_CLIENT_SECRET=${OIDC_CLIENT_SECRET}
      - OIDC_NAME=${OIDC_NAME}
      - OIDC_ENDPOINT=${OIDC_ENDPOINT}
      - OIDC_DOMAIN=${OIDC_DOMAIN}
      - ENABLE_ACME=${ENABLE_ACME}
      - ENABLE_SSH_PROVISIONER=${ENABLE_SSH_PROVISIONER}
      - SSH_HOST_PROVISIONER_PASSWORD=${SSH_HOST_PROVISIONER_PASSWORD}
      - ENABLE_K8S_PROVISIONER=${ENABLE_K8S_PROVISIONER}
    volumes:
      - ../scripts/infra-pki:/scripts
      - ./step_data:/home/step
    command: -c "/scripts/init_step_ca.sh"
    logging: *default-logging

  caddy:
    build:
      context: ./caddy
    image: infra-pki-caddy:latest # Local build, keeping latest for local
    container_name: step-ca-proxy
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      - ALLOWED_IPS=${ALLOWED_IPS}
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - ./logs/caddy:/var/log/caddy
    depends_on:
      step-ca:
        condition: service_healthy
    logging: *default-logging
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:2019/config/" ]
      interval: 30s
      timeout: 10s
      retries: 3

  fingerprint-writer:
    image: smallstep/step-cli:0.29.0
    user: "${PUID}:${PGID}"
    depends_on:
      step-ca:
        condition: service_healthy
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    volumes:
      - ./step_data:/home/step
    entrypoint: /bin/sh -c "while [ ! -f /home/step/certs/root_ca.crt ]; do sleep 1; done; step certificate fingerprint /home/step/certs/root_ca.crt > /home/step/fingerprint; echo 'Fingerprint written to step_data/fingerprint'; exit 0"
    logging: *default-logging

  watchtower:
    image: nickfedor/watchtower:latest
    environment:
      - DOCKER_API_VERSION=1.44
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

      - ./logs/watchtower:/var/log/watchtower
    command: --interval 86400 --cleanup --label-enable
    logging:
      driver: "json-file"
      options:
        max-file: "5"
        max-size: "15m"
